import fs from 'node:fs';import path from'node:path';import{AGENT_INSTRUCTIONS,CURSOR_INSTRUCTIONS}from'../agent-rules.js';import{joinLines,result}from'../output.js';
export function run(root='.',force=false){fs.mkdirSync(root,{recursive:true});const files=[['AGENTS.md',AGENT_INSTRUCTIONS],['CLAUDE.md',AGENT_INSTRUCTIONS],['.cursor/rules/agentshell.mdc',CURSOR_INSTRUCTIONS]];let created=0,updated=0,skipped=0;const lines=[];for(const[name,content]of files){const file=path.join(root,name),exists=fs.existsSync(file);if(exists&&!force){skipped++;lines.push(`skip ${file}`);continue;}fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,content);exists?updated++:created++;lines.push(`${exists?'update':'create'} ${file}`);}return result(joinLines([`agent_rules=installed created=${created} updated=${updated} skipped=${skipped}`,...lines,skipped?'suggestion=run "aish init --force" to refresh existing files':'suggestion=run "aish doctor"']));}
