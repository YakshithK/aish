import test from'node:test';import assert from'node:assert/strict';import{run,runCase,configs}from'../src/commands/benchmark.js';test('benchmark reports five evidence-preserving cases',()=>{const o=run().stdout;assert.match(o,/cases=5 evidence=preserved/);for(const name of['git_diff','pytest_failure','build_log','tree','search'])assert.match(o,new RegExp(`${name} raw_lines=`));});test('evidence fails closed',()=>{const c=['missing','pytest_failure.txt',()=> 'compact without marker\n',[['test_id','tests/test_auth.py::test_empty_password','not-present']]];const r=runCase(c);assert.equal(r.evidencePreserved,false);assert.deepEqual(r.missing,['test_id']);});
