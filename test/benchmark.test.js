import test from'node:test';import assert from'node:assert/strict';import{run,runCase,configs}from'../src/commands/benchmark.js';test('benchmark reports five evidence-preserving Python-parity cases',()=>{const o=run().stdout;assert.match(o,/cases=5 evidence=preserved/);for(const name of['git_diff','pytest_failure','build_log','tree','search'])assert.match(o,new RegExp(`${name} raw_lines=`));assert.match(o,/git_diff raw_lines=42 raw_chars=1258 compact_lines=10 compact_chars=287 shrink=77\.2%/);assert.match(o,/pytest_failure raw_lines=22 raw_chars=871 compact_lines=4 compact_chars=234 shrink=73\.1%/);});test('evidence fails closed',()=>{const c={name:'missing',fixture:'pytest_failure.txt',summarize:()=> 'compact without marker\n',evidence:[{name:'test_id',raw:'tests/test_auth.py::test_empty_password',compact:'not-present'}]};const r=runCase(c);assert.equal(r.evidencePreserved,false);assert.deepEqual(r.missing,['test_id']);});
