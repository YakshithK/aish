import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';test('package metadata uses an explicit runtime allowlist',()=>{const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));assert.deepEqual(pkg.files,["bin/","src/*.js","src/commands/","src/fixtures/","README.md","LICENSE"]);assert.deepEqual(pkg.dependencies,undefined);assert.deepEqual(pkg.devDependencies,undefined);assert.equal(fs.readFileSync('bin/aish.js','utf8').startsWith('#!/usr/bin/env node'),true);});
